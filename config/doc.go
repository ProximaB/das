// Package config assembles components to produce runnable services.
// Instead, package config assembles the system by connecting the
// atomic components within businesslogic, controller, dataaccess, and viewmodel to build the system.
// This package essentially performs the job of a framework.
package config
